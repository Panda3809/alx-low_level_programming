#include "main.h"

/**
 *  * main - Check the code
 *   *
 *    * Return: Always 0
 */
int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them -Akubada Daniel";
	print_rev(str);
	return (0);
}
