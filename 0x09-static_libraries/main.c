#include "main.h"
#include <stdio.h>

/**
 *  * main - Entry point of the program
 *   *
 *    * Return: Always 0 (Success)
 */
int main(void)
{
	char str[] = "Hello, world!";

	printf("Length: %d\n", _strlen(str));
	return (0);
}
